version: '3.8'

services:
  app:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./server:/app
      - /app/node_modules
    restart: unless-stopped
